<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Favorites

    <a data-toggle="collapse" href="#dish_favorites_new_form">
      Add favorite
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @dish.favorites.each do |favorite| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to favorite, favorite %>

        <% if current_user == favorite.user %>
          <%= link_to favorite, method: :delete do %>
            <i class="fas fa-trash-alt"></i>
          <% end %>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="dish_favorites_new_form">
        <%= render partial: "dishes/dish_favorites_new_form" %>
      </div>
    </div>

  </ul>
</div>
