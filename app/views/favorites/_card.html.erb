<div class="card mb-3">
  <h2 class="card-header h4">
    Favorite #<%= favorite.id %>
  </h2>

  <div class="card-body">
    <dl class="mb-1">
      <dt>User</dt>
      <dd>
        <% if @favorite.user.present? %>
          <%= link_to favorite.user, favorite.user %>
        <% end %>
      </dd>

      <dt>Dish</dt>
      <dd>
        <% if @favorite.dish.present? %>
          <%= link_to favorite.dish, favorite.dish %>
        <% end %>
      </dd>

      <dt>Restaurant</dt>
      <dd>
        <% if @favorite.restaurant.present? %>
          <%= link_to favorite.restaurant, favorite.restaurant %>
        <% end %>
      </dd>

    </dl>

    <div class="row">
      <div class="col">
        <%= link_to_show_or_back favorite, '<i class="fas fa-search-plus"></i>'.html_safe, '<i class="fas fa-chevron-left"></i>'.html_safe, class: "btn btn-block btn-outline-secondary" %>
      </div>

      <div class="col">
        <% if current_user == @favorite.user %>
          <%= link_to edit_favorite_url(favorite), class: "btn btn-block btn-sm btn-outline-secondary" do %>
            <i class="fas fa-edit"></i>
          <% end %>
        <% end %>
      </div>

      <div class="col">
        <% if current_user == @favorite.user %>
          <%= link_to favorite, method: :delete, data: { confirm: "Do you really want to delete this favorite?" }, class: "btn btn-block btn-outline-secondary" do %>
            <i class="fas fa-trash-alt"></i>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>

  <div class="card-footer text-muted">
    Last updated <%= time_ago_in_words(favorite.updated_at) %> ago
  </div>
</div>
